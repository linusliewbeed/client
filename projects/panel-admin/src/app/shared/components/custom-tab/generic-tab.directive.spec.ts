import { GenericTabDirective } from './generic-tab.directive';

describe('GenericTabDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericTabDirective();
    expect(directive).toBeTruthy();
  });
});

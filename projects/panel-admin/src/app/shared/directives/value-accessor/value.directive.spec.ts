import { ValueDirective } from './value.directive';

describe('ValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueDirective();
    expect(directive).toBeTruthy();
  });
});
